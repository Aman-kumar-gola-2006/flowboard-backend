spring:
  application:
    name: workspace-service
  datasource:
    url: jdbc:mysql://localhost:3306/flowboard_workspace?createDatabaseIfNotExist=true
    username: admin
    password: 05092006
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: amanagola9841@gmail.com
    password: blffrhoqqeucqqee
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8082

eureka:
  instance:
    hostname: localhost
    ip-address: 127.0.0.1
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

auth:
  service:
    url: http://auth-service/api/auth
