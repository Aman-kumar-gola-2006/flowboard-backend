# Label Service Config
# Aman - 20 April 2026

spring:
  application:
    name: label-service
  datasource:
    url: jdbc:mysql://localhost:3306/flowboard_label?createDatabaseIfNotExist=true
    username: root
    password: 05092006
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

board:
  service:
    url: http://localhost:8083/api/boards
