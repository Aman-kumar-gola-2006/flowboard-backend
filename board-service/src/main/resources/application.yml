# Board Service Config
# Aman - 18 April 2026

spring:
  application:
    name: board-service
  datasource:
    # Make sure MySQL is running buddy
    url: jdbc:mysql://localhost:3306/flowboard_board?createDatabaseIfNotExist=true
    username: root
    password: 05092006
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8083

# Eureka config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

workspace:
  service:
    url: http://localhost:8082/api/workspaces
